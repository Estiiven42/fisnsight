{
  "build": {
    "builder": "RAILPACK",
    "buildCommand": "cd servicio-autenticacion && mvn clean package"
  },
  "deploy": {
    "startCommand": "cd servicio-autenticacion && java -jar target/*.jar"
  }
}
